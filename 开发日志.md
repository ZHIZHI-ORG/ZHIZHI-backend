# 知之 ZHIZHI — 后端开发日志

**项目**：知之ZHIZHI iOS 应用后端 API
**技术栈**：TypeScript + Vercel Serverless + Supabase
**仓库**：https://github.com/jc41g/ZHIZHI-backend

---

## 已完成模块

### §3 用户认证模块 ✅

| 接口 | 端点 |
|---|---|
| 验证邀请码 | `POST /api/auth/check-invite-code` |
| 发送验证码 | `POST /api/auth/send-code` |
| 用户注册 | `POST /api/auth/register` |
| 用户登录 | `POST /api/auth/login` |
| 第三方登录 | `POST /api/auth/social-login` |
| 刷新 Token | `POST /api/auth/refresh-token` |
| 登出 | `POST /api/auth/logout` |
| 重置密码 | `POST /api/auth/reset-password` |
| 注销账户 | `POST /api/auth/delete-account` |

---

### §4 八字档案模块 ✅

| 接口 | 端点 |
|---|---|
| 创建档案 | `POST /api/bazi/create` |
| 档案列表 | `GET /api/bazi/list` |
| 档案详情 | `GET /api/bazi/:id` |
| 更新档案 | `PUT /api/bazi/:id` |
| 删除档案 | `DELETE /api/bazi/:id` |

**计算引擎**：`lunar-javascript` + 自研 `shenShaCalculator`（18个神煞查表）
**返回数据**：四柱、十神、藏干、纳音、空亡、长生十二宫、神煞、大运、五行分析
**数据库**：`bazi_profiles` 表，含 `full_chart` JSONB 字段存储完整命盘

---

### §5 首页运势模块 ✅

| 接口 | 端点 | 说明 |
|---|---|---|
| 每日运势 | `GET /api/fortune/daily` | 日主卡片 + 2个场景建议 |
| 场景详情 | `GET /api/fortune/scene/:scene` | 指定场景深度分析 + bullets |

**scene 可选值**：`career / love / health / overall / study`
**返回逻辑**：优先返回今日 AI 精选场景数据；未精选的场景从 analysis 数据构建

---

### §6 洞察分析模块 ✅

| 接口 | 端点 | 说明 |
|---|---|---|
| 轮播卡片 | `GET /api/insights/cards` | 5个维度卡片 |
| 五维分析 | `GET /api/insights/analysis` | 概览（无 bullets） |
| 维度详情 | `GET /api/insights/detail/:category` | 含 bullets 展开内容 |

**category 可选值**：`overall / career / love / health / study`

---

## 架构决策记录

### AI 内容生成策略
- 模块5和模块6共用**一次 AI 调用**生成所有数据，后端按接口拆分返回
- 避免多次调用导致内容不一致、成本高
- AI Key 留空时自动返回基于真实八字的示例数据，前端可正常开发调试

### 缓存策略
- **不做服务端缓存**，由 iOS 端按日期缓存
- 后端无状态，每次请求生成并返回，`date` 字段供前端判断是否需要刷新

### 推荐算法基础设施
- 新增 `user_interactions` 表记录用户点击行为
- 用户每次查看维度详情（`/api/insights/detail/:category`）自动异步记录
- 下次生成运势时读取权重注入 AI prompt，增加感兴趣方向的内容深度

---

## 数据库迁移文件

| 文件 | 说明 |
|---|---|
| `schema.sql` | 基础表（users、bazi_profiles 等） |
| `002_ai_interests.sql` | AI 兴趣标签 |
| `003_invite_codes.sql` | 邀请码系统 |
| `004_bazi_extended.sql` | 八字档案扩展字段（full_chart、birth_country、mbti 等） |
| `005_user_interactions.sql` | 用户行为记录表 |

---

## 踩过的坑

### 1. 本地开发代理拦截 localhost
- **现象**：curl 请求 localhost:3000 无响应
- **原因**：系统 `http_proxy=127.0.0.1:7897`（科学上网工具）拦截了 localhost 请求
- **解决**：在 `~/.zshrc` 加 `export no_proxy="localhost,127.0.0.1,::1"`

### 2. server.ts 的 mockRes 缺少 setHeader 方法
- **现象**：请求到达服务器但前端收不到响应，静默失败
- **解决**：补充 `setHeader` / `getHeader` 方法到 mockRes 对象

### 3. TypeScript 严格模式下 response.json() 类型
- **现象**：`data.candidates` 报 `'data' is of type 'unknown'`
- **解决**：`await response.json() as any`

### 4. VercelRequest 没有 params 属性
- **现象**：动态路由 `[category].ts` 读取 `req.params` 报类型错误
- **解决**：`(req as any).params?.category`

### 5. Git push 认证失败
- **现象**：`remote: Invalid username or token`
- **原因**：GitHub 已不支持密码登录
- **解决**：生成 Personal Access Token，嵌入远程地址 `https://<token>@github.com/...`

---

### §7 用户资料模块 ✅

| 接口 | 端点 | 说明 |
|---|---|---|
| 获取用户资料 | `GET /api/user/profile` | 基础字段（已有） |
| 更新用户资料 | `PUT /api/user/profile` | 基础字段（已有） |
| 获取邀请码 | `GET /api/user/invite-code` | 自动生成（首次调用） |
| 邀请记录 | `GET /api/user/invitations` | 汇总所有邀请码的使用记录 |
| 更新扩展资料 | `PUT /api/user/profile/extended` | bio / location / career / school / mbti / notes |

**邀请码逻辑**：
- 用户首次调用 `/api/user/invite-code` 时自动生成专属码（格式 `ZZ + 6位随机大写字母+数字`）
- 复用已有 `invite_codes` 表，`created_by` 字段关联用户
- `max_uses = -1`（无限次），与内测系统码统一管理

**扩展字段**：
- 新增迁移 `006_user_extended.sql`，给 `users` 表添加 `bio / location / career / school / mbti / notes` 字段
- MBTI 写入时自动校验格式（4位字母如 INTJ）并转大写

---

## 待开发接口

非 AI 对话接口已全部完成 ✅

以下接口已暂缓（AI 对话模块）：

| 模块 | 接口 | 说明 |
|---|---|---|
| AI | `POST /api/chat/send` | 自由问答 |
| AI | `GET/SSE /api/chat/stream` | 流式回复 |
| AI | `GET /api/chat/history` | 对话历史 |
| 洞察 | `POST /api/insights/followup` | AI 追问 |

---

## 本地开发说明

```bash
cd api
npm run dev:node        # 启动本地服务器（端口 3000）
```

测试接口（需先登录获取 token）：
```bash
TOKEN=$(curl -s --noproxy localhost -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"testbazi@test.com","password":"Test123456"}' \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['access_token'])")

curl --noproxy localhost http://localhost:3000/api/fortune/daily \
  -H "Authorization: Bearer $TOKEN"
```
